openapi: 3.0.3
info:
  title: LLM Query
  version: "0.0.1"
servers:
  - url: http://localhost:7080/llm
  - url: https://localhost:7443/llm
tags:
  - name: Health
paths:
  /health:
    get:
      tags: [Health]
      operationId: llmHealth
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              example: { status: "ok" }
