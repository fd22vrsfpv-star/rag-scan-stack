openapi: 3.0.3
info:
  title: ZAP
  version: "0.0.1"
servers:
  - url: http://localhost:7080/zap
  - url: https://localhost:7443/zap
tags:
  - name: Health
paths:
  /health:
    get:
      tags: [Health]
      operationId: zapHealth
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              example: { status: "ok" }
