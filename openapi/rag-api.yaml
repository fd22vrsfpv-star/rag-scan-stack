openapi: 3.0.3
info:
  title: RAG API
  version: "0.0.1"
servers:
  - url: http://localhost:7080/rag-api
  - url: https://localhost:7443/rag-api
tags:
  - name: Health
paths:
  /health:
    get:
      tags: [Health]
      operationId: ragHealth
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              example: { status: "ok" }
