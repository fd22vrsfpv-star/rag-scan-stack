fastapi==0.109.0
uvicorn[standard]==0.31.1
pydantic==2.11.0
psycopg2-binary==2.9.9
asyncpg>=0.29.0
httpx==0.27.2
h2==4.1.0
# pyautogen==0.2.18 — REMOVED (Docs/LANGGRAPH_MIGRATION_PLAN.md Phase 5).
# The agent orchestration is LangGraph; nothing imports `autogen` any more.
# `openai` below stays: langchain-openai depends on it.
openai>=1.3,<4
python-dotenv==1.0.0
mcp==1.17.0
sse-starlette==1.8.2
tenacity==8.2.3
jinja2>=3.1.2
pyyaml>=6.0

# LangGraph migration (Phase 0 spike) — supervisor orchestration + Postgres checkpointing
langgraph>=1,<2
langgraph-checkpoint-postgres
psycopg[binary]
langchain-openai
