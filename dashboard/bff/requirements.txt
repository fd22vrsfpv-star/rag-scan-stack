fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
websockets==14.1
python-multipart==0.0.20
weasyprint==63.1
jinja2==3.1.5
pydantic==2.10.4
pydantic-settings==2.7.1
sse-starlette==2.2.1
pyyaml>=6.0
httpx-sse>=0.4.0
# Direct Postgres access for status writes (e.g. scan_recommendations
# lifecycle) where round-tripping through scan_recommender would add a
# pointless HTTP hop.  psycopg2-binary bundles libpq so no
# dashboard/Dockerfile apt-get change is needed.
psycopg2-binary>=2.9
